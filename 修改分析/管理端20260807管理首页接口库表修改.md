# 管理端 20260807 管理首页接口库表修改

- 接口：无修改。
- 数据库：无修改。
- 配置数据：后台角色的预设菜单名称同步为新导航名称，资源ID与权限编码不变。

## 首页读取接口

| 接口 | 读取字段 | 首页功能 |
|---|---|---|
| `GET /api/v1/admin/logs?kind=requests` | `id, question, status, created_at, completed_at` | 指标卡、异常待办 |
| `GET /api/v1/admin/logs?kind=audit` | `action, detail, created_at, operator_id` | 通知信息 |
| `GET /api/v1/admin/phase2/providers` | `id, name, status, diagnostic_status` | 组合待办 |
| `GET /api/v1/admin/readiness` | `ready, providers.*.ready` | 系统/Provider待办 |
| `GET /api/v1/admin/approvals?status=PENDING` | `id, type, title, applicant_name, status, submitted_at, target_type, target_id` | 审批待办 |

## 关联库表字段

| 表 | 字段 | 关联功能 | 修改影响 |
|---|---|---|---|
| `requests` | `id, question, status, created_at, completed_at` | 查询量、成功率、耗时、待办 | 只读，无DDL |
| `sessions` | `id, role_id` | 查询日志接口关联操作人 | 只读，无DDL |
| `audit_logs` | `action, actor, detail, created_at` | 通知信息 | 只读，无DDL |
| `phase2_provider_profiles` | `id, name, status, public_config` | 待启用组合 | 只读，无DDL |
| `provider_diagnostics` | `profile_id, status, created_at` | 待诊断组合 | 只读，无DDL |
| `provider_configs` | `id, type, status, config` | readiness基础Provider | 通过就绪接口读取，无DDL |
| `approval_tasks` | `id, type, title, applicant_id, applicant_name, status, submitted_at, target_type, target_id` | 审批待办 | 新增表与3条POC记录 |

走势图不新增统计表，由 `requests.created_at/completed_at/status` 按日聚合。审批待办新增 `approval_tasks` 表；审批处理入口尚未启用，因此跳转按钮置灰。
