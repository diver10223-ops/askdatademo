# 用户端 20260807 修改流程分析

快捷按钮仍调用 `sceneClick → run → adapter.query`。修改只发生在 `App.vue` 展示层：用户消息绑定 `turn.question`，轨迹标题删除 `turn.scene/sceneName`。场景参数仍随查询请求发送，执行链路未改变。

修改后链路：快捷按钮 → 写入问题 → 创建Turn → 展示问题正文 → 携带场景ID提交 → SSE轨迹 → 结果区。
