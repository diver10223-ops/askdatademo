# 管理端 20260807 模型配置架构修改

前端维护提供商预设目录，负责提供商、模型和Base URL联动；后端以独立模型配置实体持久化。运行层仍统一使用 `OpenAICompatibleProvider`，由运行组合引用模型配置，避免配置耦合和复制多套七层业务代码。

厂商特有高级参数暂不进入公共运行契约，后续可通过独立Provider扩展。
## 日志读模型

模型调用日志从七层执行事实表读取，查询和会话补充角色/场景上下文，二期公开配置补充提供商与模型名称；演示模式无配置时显示模拟模型标识。该读模型不改变七层执行链路。
