# 管理端 20260807 管理首页架构修改

导航继续采用稳定ID驱动，中文名称仅作为展示元数据。公共字体规则限定在 `.admin-shell`，避免影响用户端；表格子树通过更高针对性规则维持原字号，形成全局紧凑、数据区稳定的样式边界。

首页采用前端只读聚合视图，不建立重复统计表。指标由查询日志即时计算；待办由就绪状态、运行组合状态及查询状态合并；通知由审计日志转换。所有聚合计算限制在接口返回的最近200条记录范围内，因此当前指标属于“管理视图采样统计”，后续如需全量经营统计应增加专用聚合接口。
