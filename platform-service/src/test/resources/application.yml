spring:
  datasource:
    url: jdbc:h2:mem:askdata-platform-test;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    username: sa
    password: ""
  flyway:
    enabled: true

management:
  endpoints:
    web:
      exposure:
        include: health,info
