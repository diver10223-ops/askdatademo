ASKDATA_DATA_DIR=./data
ASKDATA_HOST=0.0.0.0
ASKDATA_PORT=8000
ASKDATA_SIMULATION_DELAY_MS=80
# Phase 2 only: generate with `python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"`
ASKDATA_CREDENTIAL_KEY=
