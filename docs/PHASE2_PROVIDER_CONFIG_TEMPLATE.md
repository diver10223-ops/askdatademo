# 二期提供商配置模板

状态：配置模板；不得填写明文密钥

## 模型

配置名称、提供商、模型、Base URL、版本、温度、Top P、最大 Token、上下文窗口、结构化输出、超时、重试、并发、密钥是否已配置、状态。

## 数据源

名称、类型、环境、URL/库名、用户名、密码是否已配置、Schema、允许访问表、只读模式、字符集、连接池、最大返回行数、查询超时、状态。

密钥和密码通过后台独立表单写入加密字段，不进入本文档、日志或公开配置。
